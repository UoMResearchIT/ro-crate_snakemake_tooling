## Setting up tests

The directory `test_workflow` contains everything for running a simple Snakemake workflow, whose results are used for testing the plugin.

For running the workflow, the report plugin, and validating the resulting RO-Crate, run:

```
cd tests
./run_test_workflow.sh
```